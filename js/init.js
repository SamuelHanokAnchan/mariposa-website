/**
 * Page initialization
 * Adds 'render' class for loading animation, 'pix-loaded' for header
 */

document.addEventListener('DOMContentLoaded', () => {
  document.body.classList.add('render', 'pix-loaded');
});
